# V1 稳定化实施记录

## 2026-09-07

已完成：

- 关键词证据按规范化字符映射回原文，修复全角字符、连续空白和大小写折叠造成的定位偏移。
- AND 条件的同义词按替代项计算；没有正向条件的规则不会只凭排除词产生匹配。
- 采集任务按自身关键词组清理和写入证据，避免覆盖其他规则组结果。
- 新增公告—采集任务—关键词组绑定表，历史数据按已有命中和同平台任务补录 23 条绑定。
- 已有公告在当前任务下未命中时不计入本批成功；公告历史记录继续保留。
- 去重按当前任务绑定的关键词组判断；候选扫描不再固定限制为目标数量的五倍。
- 列表请求和详情请求均按任务重试策略重试；批次保存配置快照、心跳和停止原因。
- 附件已有有效本地文件及相同哈希时不重复下载；压缩包子文件身份包含父附件及相对路径。
- 公告原始页面按版本保存，不再覆盖固定的 `notice.html`。
- 附件下载前提交元数据事务，减少慢网络期间占用 SQLite 写锁。
- 重新解析沿用公告历史规则归属，并根据附件失败、未支持和待 OCR 状态决定 `parsed/partial`。
- 自动字段更新不覆盖已存在的人工修订字段。
- 公告列表改为后端分页，每页 10 条；搜索、平台、标记、附件和异常筛选由数据库统一执行。
- 公告库默认展示当前有正向命中的采集公告；人工导入仍保留展示。后端断开时不再显示演示公告和假管理数据。
- 关键词修改、启停、业务标记、删除及重新解析后会刷新后端数据。
- 修复开标倒计时一直显示“待确认”和浏览器加载旧脚本的问题。
- 修复 `scripts/live_acceptance.py` 无法按 README 直接运行的问题，并让三个平台独立重试和报告。

验收结果：

- 28 项隔离自动化测试通过。
- Python 与 JavaScript 语法检查通过。
- 真实 SQLite `integrity_check=ok`，265 个已登记附件路径缺失数为 0。
- 服务端分页验收：默认有效公告 5 条；搜索“服务器”返回 4 条；包含历史未匹配公告时共 15 条。
- 开标倒计时返回真实的“8/14/15 天后”及“待确认”。
- 三平台实时外网验收未通过：ECP2.0、国网交易专区连接被对端重置，南方电网 SSL 握手超时。该结果不能标记为平台采集正式通过，需在目标 Windows 网络出口复验。

备份：`backups/stabilization-20260906/`，包含优化前代码和约 14GB 数据。

尚需部署阶段完成：目标 Windows 电脑 `.exe` 构建与升级恢复测试、三平台目标网络复验、连续 72 小时运行验收、OCR 与未支持格式能力。
